* Fusion optimization. e.g. map f map g = map (f . g)
  
* List comprehension

* reduce memory usage.
  ** recursive mapping or filtering suffers from memory blowup. (I
  couldn't figure out the reason.)
